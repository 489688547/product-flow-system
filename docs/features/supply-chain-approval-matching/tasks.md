# 供应链审批匹配执行任务

## 执行规则

- 每项任务只交付一个可独立验证的结果。
- 先写失败测试并确认失败原因，再写实现。
- 完成后记录实际验证命令和结果。
- 每次提交只包含当前任务文件。

## 任务

- [x] 锁定审批匹配规则
  - 依赖：无。
  - 文件：`tests/dingtalk-approval-sync.test.mjs`。
  - 输入：采购事由、供应商档案、付款关联字段。
  - 输出：失败测试。
  - 失败测试：`node --test tests/dingtalk-approval-sync.test.mjs`。
  - 实现步骤：覆盖非采购付款、完整供应商名称、已确认别名和产品待确认。
  - 验证：测试必须因缺少新规则而失败。
  - 实际结果：新增 3 个同步规则测试和 1 个页面状态测试，均先确认失败。
  - 提交：与最小实现一并提交。

- [x] 实现并重同步测试数据
  - 依赖：锁定审批匹配规则。
  - 文件：钉钉适配器、供应链同步器和本任务文档。
  - 输入：失败测试和真实审批字段证据。
  - 输出：测试页只保留采购付款，安全提高供应商匹配率。
  - 失败测试：沿用上一任务。
  - 实现步骤：候选提取、确定性匹配、付款过滤、真实重同步。
  - 验证：聚焦测试、全量质量门禁和本地页面/API。
  - 实际结果：聚焦测试 21/21 通过；lint、治理、集成、全量测试和生产构建通过；真实同步得到 46 张采购、47 张采购付款、0 张无采购关联付款，17 张采购自动关联供应商。
  - 提交：`fix: match DingTalk supply approvals`。

- [x] 修复 Cloudflare Worker 单次同步子请求超限
  - 依赖：生产环境真实同步验收。
  - 文件：供应链审批 API、前端状态 Provider、审批客户端和契约测试。
  - 输入：生产错误 `Too many subrequests by single Worker invocation`。
  - 输出：固定时间范围内按流程、按页同步，采购页全部完成后再同步付款页。
  - 失败测试：批次测试先观察到一次读取 41 条详情而不是限定的 20 条；Provider 接线测试先观察到仍直接调用单次接口。
  - 验证：聚焦测试、完整质量门禁、生产重新同步和数据条数核对。
