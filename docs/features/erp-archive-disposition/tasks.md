# ERP 归档入库决策执行任务

## 执行规则

- 每项任务只交付一个可独立验证的结果。
- 先写失败测试并确认失败原因，再写实现。
- 完成后记录实际验证命令和结果。
- 每次提交只包含当前任务文件。

## 任务

- [x] 1. 固定持久化与领域契约
  - 依赖：DEV-000009 已由 `codex/archive-ingestion-disposition` 认领。
  - 文件：迁移、归一化领域、迁移测试。
  - 输入：现有 `erp_file_archives`。
  - 输出：决策字段、原因码、版本的兼容 schema。
  - 失败测试：迁移缺少新列；领域对象丢失决策字段。
  - 实现步骤：加迁移，扩展安全归一化，更新环境能力说明和生成清单。
  - 验证：迁移与领域聚焦测试通过。
  - 提交：与本功能最终提交合并。

- [x] 2. 完成 24 小时超时自愈
  - 依赖：任务 1。
  - 文件：storage、archives route、D1 mock、API 测试。
  - 输入：`processing` 归档的 `updated_at / archived_at`。
  - 输出：过期记录转 `failed` 和稳定错误码，SLA 内记录不变。
  - 失败测试：过期处理仍返回 `processing`。
  - 实现步骤：有界 UPDATE，GET 前执行，返回安全归档。
  - 验证：认证、存储错误、过期和未过期用例通过。
  - 提交：与本功能最终提交合并。

- [x] 3. 完成显式跳过决策 API
  - 依赖：任务 1。
  - 文件：storage、archives route、state client、API 测试。
  - 输入：归档 ID、预期版本、决策和受控原因。
  - 输出：服务端身份留痕、乐观锁、可撤销决策。
  - 失败测试：未授权、未知原因、非法状态、旧版本未被拒绝。
  - 实现步骤：PATCH 契约、storage 原子更新、客户端错误保真。
  - 验证：API 与客户端聚焦测试通过。
  - 提交：与本功能最终提交合并。

- [x] 4. 完成数据同步页面分流
  - 依赖：任务 2、3。
  - 文件：localArchive domain、LocalArchivePanel、workspace、styles、React 测试。
  - 输入：安全归档清单。
  - 输出：失败置顶、处理中中性、待决策告警、跳过折叠和权限状态。
  - 失败测试：跳过记录仍计入告警；处理中被误报警；页面没有原因操作。
  - 实现步骤：先领域分组，再组件与状态组合，最后响应式样式。
  - 验证：React 聚焦测试、1280px/390px/键盘检查。
  - 提交：与本功能最终提交合并。

- [x] 5. 写回规则并完成门禁
  - 依赖：任务 1—4。
  - 文件：ERP API、错误目录、环境能力和本任务文档。
  - 输入：最终实现。
  - 输出：迁移、错误、权限、兼容、回滚和显示策略长期规则。
  - 失败测试：环境生成清单或治理检查不一致。
  - 实现步骤：更新文档、生成 manifest、检查 diff。
  - 验证：2026-07-28 完整 Definition of Done、Pages Functions 构建与
    `check:branch-base` 通过；聚焦测试 28/28。沙箱在 1269px 与 390px
    验证超时失败、SLA 内处理中、待决策、已跳过、显式原因选择、键盘焦点、
    无横向溢出且控制台无警告或错误。
  - 提交：只包含 DEV-000009 范围。

- [ ] 6. 发布后历史记录处理
  - 依赖：PR 合并、0017 已应用、Cloudflare Git 部署成功。
  - 文件：无代码写入。
  - 输入：报告确认的精确归档 ID。
  - 输出：10 个大明细记录为 `DETAIL_STORAGE_DEFERRED`，1 个缺时间订单文件为
    `TIME_BASIS_MISSING`；其他记录不猜测。
  - 失败测试：不适用。
  - 实现步骤：通过正式 PATCH 逐条更新，复核告警与折叠数量；超时记录由 GET 自愈后回到同步任务重采。
  - 验证：真实总经办会话 GET/PATCH、生产 readiness、页面状态和请求 ID。
  - 提交：无；将证据写入 DEV-000009 验收事件。
