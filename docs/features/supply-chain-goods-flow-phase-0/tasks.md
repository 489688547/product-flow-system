# 供应链管理货流升级执行任务

## 执行规则

- 每项任务只交付一个可独立验证的结果。
- 先写失败测试并确认失败原因，再写实现。
- 完成后记录实际验证命令和结果。
- 每次提交只包含当前任务文件。
- 当前任务表用于记录设计门禁；书面设计复核通过后再按详细实施计划拆分代码任务。

## 设计门禁

- [x] 核对现有供应商、采购付款、库存盘点、库存资金、销售成本和商品主数据边界。
- [x] 确认应收天数由财务按平台维护固定账期，并按生效日版本化。
- [x] 确认 ERP 账面库存与月度线下盘点实存使用双账本。
- [x] 确认保留“供应链管理”唯一入口，并抽取独立货流数据平台。
- [x] 确认页面层级、角色权限、异常处理和 Phase 0 验收标准。
- [x] 复核本目录 PRD、设计、边界计划和架构决策，批准进入详细实施计划。

## 后续实施任务组

- [x] 货流领域公式与契约测试。
  - 依赖：书面设计复核通过。
  - 输入：本目录 PRD 与设计书。
  - 输出：库存校准、CCC、覆盖率和异常的纯领域契约。
  - 失败测试：覆盖盘点锚点、缺失来源、账期版本和冻结重算。
  - 验证：聚焦领域测试通过。
  - 实际验证：`node --test react-tests/goods-flow-domain.test.mjs`（5 项通过）；`npx eslint src/domain/goodsFlow.js react-tests/goods-flow-domain.test.mjs`。

- [x] D1 数据模型、迁移和环境能力。
  - 依赖：领域契约。
  - 输入：货流事件、日库存、盘点、账期、CCC 和异常表定义。
  - 输出：可回滚迁移、环境清单、就绪检查和存储契约。
  - 失败测试：未迁移、重复迁移、缺表和权限错误。
  - 验证：迁移测试及环境能力检查通过。
  - 实际验证：`node --test tests/goods-flow-migration.test.mjs tests/environment-capabilities.test.mjs`（8 项通过）；`npm run check:integrations`；`npm run check:environment-capabilities`。

- [x] 多来源接入与兼容投影。
  - 依赖：D1 数据模型。
  - 输入：现有供应链状态、钉钉审批、快麦销售和 ERP/文件库存。
  - 输出：幂等事件、商品映射异常和迁移对账报告。
  - 失败测试：重复导入、付款未关联、SKU/69 码缺失和部分失败。
  - 验证：API 契约和真实样本脱敏测试通过。
  - 实际验证：兼容投影、付款关联、SKU/69 码异常、部分成功和无写入迁移预览测试通过；预览报告只输出计数、覆盖率、未映射项和阻断差异。

- [x] 库存、盘点与 CCC API。
  - 依赖：多来源接入。
  - 输入：货流事件、平台账期和盘点确认。
  - 输出：日库存投影、盘点状态机、月度 CCC 版本和冻结结果。
  - 失败测试：角色越权、版本冲突、历史重算和来源覆盖不足。
  - 验证：API 认证、权限、失败、超时和兼容测试通过。
  - 实际验证：盘点金额确认追加库存调整事实并重建校准投影；CCC 只从服务端库存、事件和账期事实重算，忽略客户端明细。货流 API、存储和投影测试通过。

- [x] 供应链管理 UI 合并升级。
  - 依赖：稳定 API 契约。
  - 输入：驾驶舱、库存、盘点、现金循环和异常读模型。
  - 输出：一个供应链管理入口及货流七环节左侧 Tab。
  - 失败测试：加载、空数据、错误、无权限、禁用、过期和部分成功状态。
  - 验证：键盘、焦点、WCAG AA、笔记本宽度、响应式及钉钉 WebView 通过。
  - 实际验证：1440、1180、768 本地页面完成浏览器检查；10 个左侧 Tab、3 个驾驶舱指标、无返回按钮、无嵌套标题卡、表头横排、表格滚动、空/禁用/缺来源状态和采购页 Tab 切换通过。

- [ ] 回填、对账、开关和三环境验收。
  - 依赖：UI 与 API 完成。
  - 输入：现有生产供应链数据和受控生产数据网关。
  - 输出：回填报告、指标对账、回滚证据和发布就绪结论。
  - 失败测试：覆盖率异常、旧新指标差异、同步失败和回退路径。
  - 验证：完整质量门及本地、Preview、Production 分别通过。
  - 当前进度：无写入迁移预览已完成；正式 `npm start` 的真实数据回填、Preview、Production 回填与发布未执行，本次开发请求不包含部署授权。
