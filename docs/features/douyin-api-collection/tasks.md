# 抖音接口采集 任务

## 1. 生产验证

- [x] 确认 DOM 路线不可行并回滚（#159 → #160）
- [x] 抓取四类资源的接口路径
- [x] 验证鉴权方式与必需参数
- [x] 确认响应结构与成功判据
- [x] 修正误登记的 `core_index_v3` 路径

## 2. 域逻辑

- [x] `douyinApi.js`：接口清单、URL 构造、响应判定
- [x] 单元测试覆盖四类失败信号
- [x] 生产实测：两个接口 × 两个业务日全部取到数据

## 3. 接入执行器

- [ ] 以接口取数替代 `applyBusinessDate` 的 DOM 操作
- [ ] 字段映射到现有标准事实
- [ ] 分页资源的翻页与终止条件

## 4. 文档与闸门

- [ ] `docs/platform/error-codes.md` 登记新增错误码
- [ ] 六项闸门与 `npm run check:pr`
