# 抖音电商罗盘摸底

日期：2026-07-29。全部结论在生产页面上实测得出，未经实测的一律标注为未知。

## 为什么要先摸底

罗盘的三个资源（商品卡、直播、短视频）每天都报
`DOUYIN_DATE_RANGE_NOT_APPLIED`，而店铺首页每天都成功。逐个接口打补丁只会
不断踩坑：同一个「查不到数据」的现象，可能是日期参数选错、页面保留期不同、
或者必填参数缺失，三者的处置方式完全不同。

## 页面为什么补不了历史日

罗盘的时间控件是**预设按钮 + 范围选择器**，没有可写值的输入框：

```
2026/07/28 – 2026/07/28  [实时][近1天][近7天][近30天][自然日][自然周][自然月][大促] [←][→]
```

- 「自然日/自然周/自然月」是**统计粒度**，不是时间范围
- 要取指定历史日只能打开范围选择器去点，而罗盘日期组件只响应 `isTrusted` 事件
- URL 上的 `date_value` / `date_type` **不生效**：实测传 07-27 单日，页面仍显示
  默认的近 7 天。快麦那条「把日期塞进 URL」的解法在罗盘上不成立

店铺首页每天成功，是因为它取默认范围，不需要改日期。

## date_type 是取数的关键

后端按 `date_type` 校验日期，选错会被拒或只给最近两天：

| `date_type` | 含义 | 取三天前（07-26） |
|---|---|---|
| 20 | 页面「近1天」 | `st:100008` 日期校验失败 |
| 21 | 自定义范围 | 正常返回 |

日期格式不敏感：`2026/07/26 00:00:00`、`2026-07-26`、`2026/07/26` 三种写法
在 `date_type=21` 下返回完全相同的结果。

## 罗盘自己会告诉你能查多久

```
GET /compass_api/config_center/data_range_v2?data_type=<页面标识>&path=<页面路径>
```

返回 `data_range_map`，按 `date_type` 给出允许的日期窗口。直播概览页实测：

| `date_type` | 可查范围 | 跨度 |
|---|---|---|
| 22 | 2026-07-26 ~ 07-28 | 3 天 |
| 21 | 2026-07-22 ~ 07-28 | 7 天 |
| 23 | 2026-06-29 ~ 07-28 | 30 天 |
| 24 | 2026-04-30 ~ 07-28 | 90 天 |
| 7 | 2026-01-01 ~ 07-28 | 今年至今 |

**采集器应当先查这个接口，再决定用哪个 `date_type`，以及目标业务日是否可采**，
而不是固定一个值然后盲目重试。这个接口按页面返回，各页面窗口并不相同。

## 各资源实测结论

| 资源 | 页面 | 可回溯 | 依据 |
|---|---|---|---|
| `live_daily` | `/shop/live-overview` | **约 90 天** | `date_type=24` 实测取到 07-15、07-20，且逐日数值不同 |
| `product_daily` | `/shop/merchandise-traffic` | **仅约 3 天** | 07-25 及更早在 `date_type=21` 与 `24` 下均返回 0 行 |
| `store_daily` | `/shop` | 未测 | 现有采集每天成功 |
| `video_daily` | `/shop/video/overview` | 未测 | 参数尚未抓取 |

商品卡的短保留期不是故障，补不了就是补不了，不应按失败重试。

## 已验证的接口与必填参数

参数取自页面实际请求，缺失会判「参数校验失败」，不是静默降级。

### 商品卡列表
```
GET /compass_api/shop/product_card/channel_product/channel_product_card_list
date_type, begin_date, end_date, activity_id, is_activity, category_code,
product_status, is_asc, channel, product_tab, only_abnormal, page_no, page_size
```
数据在 `data`（数组）。

### 直播间明细
```
GET /compass_api/shop/live/live_overview/live_room_detail_v2
date_type, begin_date, end_date, page_no, page_size, index_selected, a_type, activity_id
```
数据在 `data.module_data.shop_live_list_room_detail.compass_general_table_value.data`。

`index_selected` 决定返回哪些指标，其中 **`ad_costed_amt` 是投放消耗、
`stat_cost` 是广告花费**——广告费用就在这个接口里，不必另找数据源。

## 日期是否真正生效，必须按数值验证

「有返回」不等于「是那天的数据」。逐日比对首行支付金额：

| 资源 | 07-26 | 07-27 |
|---|---|---|
| 商品卡 | 1,519,911 | 1,441,483 |
| 直播 | 1,051,229 | 998,353 |

数值逐日不同，才算日期真正参与了查询。

## 十三个模块与页面路由

| 模块 | 落地路径 | 子页面 | 有下载明细 |
|---|---|---|---|
| 首页 | `/shop` | 核心数据 | 否 |
| 交易 | `/shop/business-part` | 全店成交分析、全店退款分析、**收支投放分析**、账号成交分析 | 是 |
| 直播 | `/shop/live-overview` | 直播概览及列表、直播复盘、主播分析、直播计划、直播对比 | 是 |
| 短视频 | `/shop/video/overview` | 视频概览、自营视频、合作视频、图文分析、视频榜单、视频达人榜 | 是 |
| 商品卡 | `/shop/merchandise-traffic` | 商品卡分析、商品卡列表、流量分析、竞店对比、竞品对比 | 是 |
| 搜索 | `/shop/search/traffic-analysis` | 流量概况 | — |
| 达人 | `/shop/talent-core-analysis` | 达人概览、达人推荐 | 否 |
| 商品 | `/shop/commodity/product-list` | 商品列表、流量分析、行业竞对、商品机会、商品榜单 | 是 |
| 营销 | `/shop/activity-analysis` | 活动总览、大促作战室、活动大屏、频道分析、营销工具分析 | 是 |
| 体验 | `/shop/ecology/experience-score` | 商家体验分、售后分析、客服分析、用户原声、物流分析 | 否 |
| 人群 | `/shop/core-users` | 本店人群洞察、用户偏好 | 否 |
| 市场 | `/shop/chance/rank-shop` | 市场排行、店铺/商品/内容/搜索/达人榜单、行业机会 | 否 |
| 数据工厂 | `/shop/workshop/appcustom-access` | 应用定制、**自助取数** | — |

**支出与广告投放费用有两个来源**：交易模块的「收支投放分析」页，以及直播明细接口
的 `ad_costed_amt`（投放消耗）与 `stat_cost`（广告花费）指标。

## 自助取数才是正确的采集通道

`数据工厂 → 自助取数`（`/shop/workshop/appcustom-access`）是官方的批量取数入口，
能力远超逐页抓接口：

- **最长支持近 14 个月数据下载**，对比：直播接口 90 天、商品卡仅约 3 天
- 主要维度：店铺、商品、直播间、短视频；次要维度：类目、载体、账号类型、账号名称
- 时间粒度：统计日期累计、自然日累计、自然周累计、自然月累计
- 单次统计周期最长 3 个月
- 指标按类型分组：成交、结算、退款、**支出**、流量、体验分
- 成交类指标包含：成交金额、用户支付金额、净成交金额、投放贡献成交金额及占比、
  新老客支付金额与订单数等
- 异步任务式：创建任务 → 任务列表 → 下载，另有「常用模版」可复用配置

这条通道一次覆盖店铺、商品、直播、短视频四个维度，且能回溯 14 个月，
应当优先于逐页接口。逐页接口只在需要当日实时数据时才有必要。

## 自助取数的实际流程（已跑通一次）

2026-07-30 实际创建了一个任务，全程验证如下。

表单必填项：任务名称（默认「新建任务MM/DD HH:mm:ss」）、主要维度（店铺/商品/
直播间/短视频 单选，值为 `shop`/`product`/`live`/`video`）、时间粒度、统计周期、
至少一个指标。

创建后自动跳到「任务列表」，一行任务含：任务名称、创建人、任务状态、创建日期，
操作列有「查看 / 下载 / 删除」。状态显示为「排队中 78/78」——**全平台共用队列**，
页面提示取数完成一般需要 10-20 分钟。

因此自助取数是异步的，采集器必须按「创建 → 轮询任务列表 → 下载」三段来做，
不能指望同步拿到文件。队列长度会随平台繁忙程度变化，超时判定要留足余量。

### 日期只能点，不能填

统计周期的输入框接受打字，但**打进去的值不进表单模型**：输入 `2026-07-25`
后显示正常，提交时报「请输入时间」。按回车更糟——会清空输入并打开日历面板。

用合成事件点日历格子同样无效：显示变成 `2026/07/25 - 2026/07/29`，校验仍报
「请输入时间」。只有**真实鼠标点击日历格子**才写入模型，点完校验错误立即消失。

这与快麦销售报表的只读日期选择器是同一类问题：**看得见的值不等于提交的值**。
凡是日期控件，都必须以「提交是否通过」或「实际请求参数」为准，不能看输入框。

### 请求抓不到，模块疑似跑在独立沙箱

改写 `window.fetch` 与 `XMLHttpRequest` 都抓不到该模块的请求，
`performance.getEntriesByType("resource")` 里也只有主框架的接口。该模块加载的是
`lf6-cdn2-tos.bytegoofy.com/gftar/cmp/...` 下的独立入口，疑似微前端沙箱，
主框架看不到它的网络活动。

因此**接口契约暂时拿不到**，短期内自助取数只能靠页面操作驱动；若要接口化，
需要另找抓取手段（如扩展的 `webRequest` 权限或本机代理）。

## 尚未摸清

- 自助取数三个接口的契约：主框架抓不到，需换手段（扩展 webRequest 或本机代理）
- 自助取数下载文件的实际格式与字段
- 自助取数导出文件的字段，与现有标准事实的映射
- `video_daily` 的接口与必填参数
- 各页面的 `data_type` 标识（商品卡页的猜测值返回「调用下游失败」）
- 各页面「下载明细」的导出文件与接口直读的字段差异
- 收支投放分析页的接口与字段
