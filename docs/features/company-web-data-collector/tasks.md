# 公司网页数据采集器任务清单

- [x] 1. 建立上海时区日计划、幂等键、状态转换、游标和通知纯领域模型。
- [x] 2. 新增通用 D1 runner/job/run/cursor/notification 表及认证 API。
- [x] 3. 建立日常 Chrome MV3 插件、本机安全桥接、调度、下载、Keychain、LaunchAgent 和系统通知运行时。
- [ ] 4. 完成快麦 ERP adapter：订单、销售明细、商品、库存、采购、供应商、售后、店铺、仓库、销售分析、进销存和库存成本。
  - [x] 4.1 支持快麦“订单页创建异步导出 → 下载中心精确匹配 → 点击官方文件下载”，消除已生成文件被误报下载超时。
  - [x] 4.2 支持《销售主题分析-按订单商品明细》按创建时间自动导出、日期二次校验、归档并投影退款/成本完整销售事实。
  - [ ] 4.3 快麦订单导出前自动确认非个人经营字段，不再继承仅含少数字段的默认模板；完成真实文件、归档和 D1 入库验收后勾选。
- [ ] 5. 数据同步页面读取真实设备、任务、运行、游标和通知状态，并保留异常推断回退。
- [ ] 6. 通过完整门禁，获得执行时授权后完成 D1/Pages、公司 Mac 和真实快麦三条验收链路。
- [ ] 7. 瞬时失败自动退避重试，适配器升级生成新版本任务，数据同步页按真实资源提供人工兜底。

## 完成证据

- 本地测试：插件、快麦适配、loopback、调度、控制面、下载与运行时 33 个定向测试通过；`lint`、治理、集成、环境、完整 `npm test` 和生产构建通过。
- 自动恢复：退避、3 次上限、版本升级与按资源重试的定向测试已完成；网页采集专项现为 48 个测试通过，生产任务重跑仍待 GitOps 部署后验收。
- D1 / Pages：待执行时授权。
- 公司 Mac LaunchAgent：待安装并核对当前仓库路径。
- 快麦真实页面结构：已在日常 Chrome 登录态只读确认订单页、下单时间、起止时间、查询、导出订单和导出订单明细控件；未触发真实导出。
- 快麦真实导出与入库：2026-07-22 `order_items` 已完成 9,882 条自动归档入库；`sales_items` 已由插件自动导出、归档并在 2026-07-23 18:37 完成 D1 ingest。`orders` 因快麦默认模板只含 8 列被本机预检安全拦截，字段选择修复仍待发布后完成真实验收；其他资源仍未适配。
