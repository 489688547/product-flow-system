# 抖店罗盘经营数据采集任务

## 执行规则

- 每项先写失败测试并确认失败原因，再实现最小代码。
- 一次只推进一个共享边界，Kuaimai 兼容测试必须持续通过。
- 不使用子代理；在当前工作树按计划连续执行。
- adapter 未完成真实样本验收前不得显示“已接通”。

## 任务

- [x] 确认范围和设计
  - 输出：店铺、商品、直播、短视频昨天数据；官方下载优先；首版不补历史。
  - 验证：用户确认设计提案。
- [x] 固化治理、ADR、integration 与环境能力
  - 输出：旧凭据登录保持退役、已登录 Chrome 只读采集进入 `integrating`，登记 D1 表与展示策略。
  - 失败测试：缺少 ADR、路径影响或环境模块时治理检查失败。
- [x] 扩展 store-scoped 网页采集控制面
  - 输出：Douyin 四资源、storeId job/cursor、目标环境版本、稳定错误和日计划。
  - 失败测试：不同店铺任务冲突或未知字段被接受时失败。
- [x] 实现标准经营事实与批次 API
  - 输出：批次安全提交、四类事实表、只读查询、质量和派生率。
  - 失败测试：未完成批次可见、缺失值补零、业务数据库绕过或旧环境版本写入时失败。
- [x] 实现 Douyin 文件解析与本机 processor
  - 输出：安全样例 fixtures、四资源字段映射、日期/店铺/行数校验、原始归档和 ingest。
  - 失败测试：未知格式、错日期、缺字段、重复文件或敏感字段进入 payload 时失败。
- [x] 实现 Douyin MV3 adapter
  - 输出：固定 host/page/action、下载、`store_daily` 安全读数、登录与人工验证状态。
  - 失败测试：远端 URL/selector、未知 task 字段、任意页面内容或未登记 host 被接受时失败。
- [x] 抽取通用 orchestrator processor
  - 输出：服务端按已登记店铺生成日计划；Kuaimai 与 Douyin 共用任务执行，支持 `downloaded`/`captured`；本机按 provider 归档、解析、上传完整批次，失败不推进游标。
  - 失败测试：采集结果未入库却完成任务、失败无同步记录或 Kuaimai 回归时失败。
- [x] 接入数据接入和数据同步 UI
  - 输出：真实 readiness、四资源执行记录、异常优先和人工恢复；巨量千川仍未接入。
  - 失败测试：显示虚假成功、筛选自动请求、错误无恢复动作或窄屏溢出时失败。
  - 实际结果：数据接入仅在全部店铺四资源存在真实成功批次时显示已接通；数据同步按店铺和资源列出状态与恢复动作；1440、1180、640、390px 本地浏览器复核无页面横向溢出。
- [x] 完成昨天真实验收
  - 输出：已登录店铺四资源的真实结果、selector/report version、原始归档和 D1 查询证据。
  - 验证：成功资源有完成批次，失败资源有安全错误和可执行恢复动作。
  - 2026-07-24 现场证据：已从固定抖店管理页识别店铺 `90862283 / TIYES提野星宠物用品旗舰店`；店铺总览 `selectorVersion=2026-07-24` 成功读取 11 个白名单原子指标。
  - 官方报表解析：`douyin-product-v2` 105 行、`douyin-live-v2` 226 行、`douyin-video-v2` 1131 行；原始 XLSX 已按店铺、资源、业务日和内容哈希归档到公司 Mac。
  - 生产 D1 结果：2026-07-23 店铺 1 行、商品 105 行、直播 226 行、短视频 1131 行均为完成批次并推进四资源游标。
- [x] 完成全量门禁和 GitOps 发布
  - 输出：全量检查、PR、main 合并、Cloudflare Git 部署、生产 API/UI 与插件复核。
  - 验证：Definition of Done 和 production readiness 全部通过。
  - 2026-07-24 发布证据：本地 `lint`、治理、集成、环境能力、完整测试与生产构建均通过；PR #82
    已合并并经后续生产版本验证，抖店完成批次可从正式 D1 查询。
- [x] 优化数据接入与多店铺识别
  - 删除数据接入摘要/刷新块、抖音资源标签、运行状态说明和“打开抖店处理”。
  - 抖音卡片展示全部已登记店铺名称与店铺 ID，底部操作为“添加”。
  - 已授权管理员通过服务端目录幂等新增或更新店铺；非法 ID、无 runner、无权限均稳定失败。
  - 不同登录账号使用独立 Chrome Profile；扩展按当前 Profile 已识别店铺 ID 领取 Douyin 任务，避免串店。
  - 按 TDD 完成控制面、客户端、Bridge、orchestrator、extension、UI 与响应式验证。
- [x] 确认专用浏览器重构设计
  - 决策：公司 Mac 使用非默认数据目录的持久可见 Chrome Profile，MV3 扩展退出长期长任务主链路。
  - 分工：Chrome 只做登录页面内短操作、官方报表下载和固定白名单指标读取；本机承担全部耗时任务。
  - 安全：本机通过受控 API 入库，Chrome 和本机都不能直接持有或选择 D1 binding。
  - 迁移：抖店先行、插件互斥回滚、连续运行 7 天后再停止插件自动领取。
- [x] 实现 Profile registry 与 browser supervisor
  - 失败测试：默认个人 Profile、未知账号、重复 Profile、非登记数据目录或非本机浏览器控制被接受时失败。
  - 输出：Profile 状态、可见 Chrome 启停、心跳、进程恢复和店铺核对。
- [x] 迁移 Douyin 页面短操作
  - 失败测试：任意 URL/脚本/selector、错店、错日期、未登录或页面结构变化仍继续采集时失败。
  - 输出：四资源固定页面、官方报表优先、`store_daily` 白名单读数和稳定错误码。
- [x] 实现本机长任务检查点
  - 失败测试：服务或 Chrome 重启后重复写入、任务永久运行、失败覆盖可信批次或人工验证消耗重试时失败。
  - 输出：等待、下载稳定、归档、解析、校验、分块上传、重试和断点恢复。
- [x] 实现本地加密诊断与 7 天清理
  - 失败测试：非登记页面截图、诊断上传 D1、绝对路径外泄、过期文件未删除时失败。
  - 输出：失败截图、脱敏结构指纹、本地诊断编号和自动清理。
- [x] 完成新旧执行器互斥与数据同步状态
  - 失败测试：插件和专用浏览器重复领取、UI 不区分设备/Profile/页面/本机处理故障时失败。
  - 输出：执行器版本租约、Profile 状态、检查点、人工动作和截止告警。
- [ ] 完成专用浏览器生产验收
  - 使用 2026-07-23 完成批次回归并采集最新业务日。
  - 连续观察 7 个业务日；成功、失败和人工恢复均有记录，D1 不出现半批。
  - 达标后停止 MV3 扩展自动领取，再按同一架构迁移快麦。
  - 2026-07-25 本机证据：专用店铺 Profile 已用独立数据目录启动并可复用，DevTools 仅监听本机；未登录报表会稳定识别为 `DOUYIN_LOGIN_REQUIRED`，公开官网和登录前空壳不再误报 ready。
  - 当前阻塞：生产控制面尚未发布 `assigned_stores` 动作，线上返回 `WEB_COLLECTION_ACTION_INVALID`；因此常驻进程已切换专用模式，但在 GitOps 发布前不会领取抖店任务。
