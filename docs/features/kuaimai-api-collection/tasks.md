# 快麦订单接口采集 任务

## 1. 生产验证

- [x] 确认源头数据完好（07-25/26/27 分别 6556/6625/6732 单）
- [x] 定位接口与请求格式
- [x] 确认无签名、仅依赖 Cookie
- [x] 确认 `data.total` 位置
- [x] 确认 `timeType=created` 为正确口径，且无效值静默回落

## 2. 域逻辑

- [x] `kuaimaiApi.js`：参数构造与口径白名单
- [x] 响应判定与分页终止条件（拉不满即整批作废）
- [x] 错误码登记与人话翻译
- [x] 单元测试（11 条）
- [ ] **`/trade/search` 的行数据结构尚未实测**。已记录的
      `page.offsetRow/pageNo/pageSize/startRow` 来自 count 接口，
      列表接口把订单放在哪个字段下还没验证过，因此本次不实现行提取

## 3. 接入与补数

- [ ] 接入执行器
- [ ] 补齐 07-25 至 07-27
- [ ] 六项闸门与 `npm run check:pr`
