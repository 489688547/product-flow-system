# 快麦订单接口采集 任务

## 1. 生产验证

- [x] 确认源头数据完好（07-25/26/27 分别 6556/6625/6732 单）
- [x] 定位接口与请求格式
- [x] 确认无签名、仅依赖 Cookie
- [x] 确认 `data.total` 位置
- [x] 确认 `timeType=created` 为正确口径，且无效值静默回落

## 2. 域逻辑

- [x] `kuaimaiApi.js`：参数构造与口径白名单
- [x] 响应判定与分页终止条件（拉不满即整批作废）
- [x] 错误码登记与人话翻译
- [x] 单元测试（11 条）
- [x] `/trade/search` 行数据结构实测：`data.list` + 子表 `orders`
- [x] 字段映射，沿用导出文件列名，接口与文件两条路线产出同一形状
- [x] 确认 `cost` 是单价成本，必须乘数量
- [x] 在 200 行生产数据上验证映射

## 3. 接入与补数

- [ ] 接入执行器（下一步）
- [ ] 核实导出文件「毛利润」是否扣运费，再决定是否推导
- [ ] 确认 `source = sys` 是否为 PRODUCT.md 要排除的「其它」
- [ ] 补齐 07-25 至 07-27
- [ ] 六项闸门与 `npm run check:pr`
