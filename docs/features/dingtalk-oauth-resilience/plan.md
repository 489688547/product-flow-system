# 钉钉 OAuth 冷启动韧性实施计划

1. 以失败测试锁定 Pages 静态路由、重试边界和构建产物。
2. 抽取轻量登录开始边界，新增 JSON bootstrap。
3. 复用原登录完成逻辑，新增 JSON complete。
4. 新增静态开始/回调页和浏览器重试模块。
5. 将两个公开入口排除出 Pages Functions，并确保构建保留 `_routes.json`。
6. 扩展生产就绪检查，验证静态入口、冷启动恢复与并发。
7. 通过 GitOps 合并部署，使用新浏览器会话与生产并发请求验收。

回滚时删除静态排除规则，旧 start/callback 302 路由仍保留，可恢复原流程。
